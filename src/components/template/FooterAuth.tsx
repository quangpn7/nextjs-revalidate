import React from "react";

const FooterAuth = () => {
  return (
    <div
      className="w-100 text-white bg-warning"
      style={{
        height: "50px",
        lineHeight: "50px",
      }}
    >
      Footer Auth
    </div>
  );
};

export default FooterAuth;
